import { ReportArtifact } from '../../types/api';

// For now, no specific request/response types are needed beyond what's in api.ts
// But we keep the file for future domain-specific schema definitions.
export type { ReportArtifact };
